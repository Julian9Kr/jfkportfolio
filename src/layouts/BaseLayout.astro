<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Julian F. Krueger – AI Visuals</title>
    <style>
      * {{
        box-sizing: border-box;
      }}
      body {{
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, sans-serif;
        background: #0e0e0e;
        color: white;
        overflow-x: hidden;
      }}
      .hero {{
        position: relative;
        text-align: center;
      }}
      .hero img {{
        width: 100%;
        height: auto;
        max-height: 80vh;
        object-fit: cover;
      }}
      .hero-text {{
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        color: white;
        background: rgba(0, 0, 0, 0.4);
        padding: 1rem 2rem;
        border-radius: 12px;
      }}
      .hero-text h1 {{
        margin: 0;
        font-size: 2rem;
      }}
      .gallery {{
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
        gap: 2rem;
        padding: 4rem 2rem;
        max-width: 1400px;
        margin: 0 auto;
      }}
      .image-wrapper img {{
        width: 100%;
        height: auto;
        aspect-ratio: 16 / 9;
        object-fit: cover;
        border-radius: 12px;
      }}
    </style>
  </head>
  <body>
    <slot />
  </body>
</html>
